---
type Props = {
    title: string;
    percent: number;
    hidden?: boolean;
};

const { title, percent, hidden = false } = Astro.props;
---

<li
    class={`skill-card group flex flex-col gap-1 ${hidden ? "opacity-20 cursor-not-allowed" : ""}`}>
    <h3
        class={`text-xl font-light text-white/70 transition-colors text-shadow-black text-shadow-md ${hidden ? "" : "group-hover:text-white"}`}>
        {title}
    </h3>
    <div class="h-2 w-full max-w-xl bg-white/10 p-0.5">
        <div
            class="h-full origin-left scale-x-0 bg-linear-0 from-[#6095FF] to-[#4180FF] transition-transform duration-1000 group-[.visible]:scale-x-100"
            style={{ width: `${percent}%` }}>
        </div>
    </div>
</li>

<script>
    const observer = new IntersectionObserver((entries) => {
        for (const entry of entries) {
            (entry.target as HTMLElement).classList.toggle(
                "visible",
                entry.isIntersecting
            );
        }
    });

    const skillCards = document.getElementsByClassName("skill-card");

    for (const card of skillCards) {
        observer.observe(card);
    }
</script>
